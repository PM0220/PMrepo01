pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('call this job only') { 
            steps { 
                sh 'echo "this is first job"' 
            }
        }
    }
}
